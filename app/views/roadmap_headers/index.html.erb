<% provide(:title, 'All Roadmaps') %>
<h1>All Roadmaps</h1>

<ul class="roadmaps">
  <% @roadmap_headers.each do |r| %>
    <li>
      <%= link_to r.title, roadmap_show_path(r) %>
    </li>
  <% end %>
</ul>